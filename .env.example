# =============================================================================
# SALESFORCE — EDIT THESE WHEN CREDENTIALS CHANGE
# =============================================================================
# Auth priority: SF_ACCESS_TOKEN → sf CLI (SF_ORG_ALIAS) → SF_USERNAME/SF_PASSWORD.
# Set only one of these auth methods.

# Option A — access token (recommended for Docker/launchd; no CLI required).
# Get values with: sf org display --target-org CHProd --json
SF_ACCESS_TOKEN=
SF_INSTANCE_URL=https://yourorg.my.salesforce.com

# Option B — username / password (fallback).
SF_USERNAME=you@yourorg.com
SF_PASSWORD=yourpassword
SF_SECURITY_TOKEN=yoursecuritytoken
SF_DOMAIN=login          # 'test' for sandbox, 'login' for production

# Org alias used by the sf CLI path (only works if `sf` is on PATH).
SF_ORG_ALIAS=MyOrg

# =============================================================================
# CLICKHOUSE CLOUD — EDIT THESE WHEN CREDENTIALS CHANGE
# =============================================================================
# Used by ingest scripts (CH_ prefix, with https://)
CH_HOST=https://abc123.us-east-1.aws.clickhouse.cloud
CH_PORT=8443
CH_USER=default
CH_PASSWORD=yourpassword
CH_DATABASE=salesforceProd

# Used by LibreChat MCP and Grafana (CLICKHOUSE_ prefix, no https://)
CLICKHOUSE_HOST=abc123.us-east-1.aws.clickhouse.cloud
CLICKHOUSE_PORT=8443
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=yourpassword
CLICKHOUSE_SECURE=true
CLICKHOUSE_DATABASE=salesforceProd

# =============================================================================
# LIBRECHAT — generate once with: openssl rand -hex 32
# =============================================================================
JWT_SECRET=generate-with-openssl-rand-hex-32
JWT_REFRESH_SECRET=generate-with-openssl-rand-hex-32
CREDS_KEY=generate-with-openssl-rand-hex-32
CREDS_IV=generate-with-openssl-rand-hex-16
MEILI_MASTER_KEY=generate-with-openssl-rand-hex-32
ANTHROPIC_API_KEY=sk-ant-api03-...

ALLOW_EMAIL_LOGIN=true
DEBUG_LOGGING=false
NO_INDEX=true
UID=1000
GID=1000

# =============================================================================
# GRAFANA
# =============================================================================
GRAFANA_ADMIN_PASSWORD=change-me-on-first-login

# =============================================================================
# POSTGRES (LibreChat RAG vector store — internal Docker network only)
# =============================================================================
POSTGRES_DB=librechat_rag
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=generate-with-openssl-rand-hex-16
